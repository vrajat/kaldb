# Reports

Benchmark runs write their generated human-readable report as `report.md` inside
the per-run artifact directory. This folder exists only to reserve the reports
namespace in source control; it does not contain static report templates.
